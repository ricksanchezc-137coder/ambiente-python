[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "meupacote"
version = "0.1.0"
description = "Pacote de teste - backend hatchling"
requires-python = ">=3.9"
