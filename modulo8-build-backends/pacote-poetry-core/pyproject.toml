[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[project]
name = "meupacote"
version = "0.1.0"
description = "Pacote de teste - backend poetry-core"
requires-python = ">=3.9"
